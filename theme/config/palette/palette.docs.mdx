---
description: 'exports a set of css variables for each color theme'
labels: ['theme', 'css-module']
---

import * as palette from '@watheia/theme.config.palette';

```jsx
() => <>{JSON.stringify(palette)}</>;
```
